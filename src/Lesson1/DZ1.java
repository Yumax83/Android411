package Lesson1;

public class DZ1 {
    public static void main(String[] args) {
        System.out.println("Домашнее задание" + " " + "№1");
        System.out.println("a). Переменная типа byte занимает " + Byte.BYTES + " байт памяти ("
                + Byte.SIZE + " бит памяти)," + " и принимает значения в диапазоне от "
                + Byte.MIN_VALUE + " до " + Byte.MAX_VALUE);
        System.out.println("b). Переменная типа short занимает " + Short.BYTES + " байта памяти ("
                + Short.SIZE + " бит памяти)," + " и принимает значения в диапазоне от "
                + Short.MIN_VALUE + " до " + Short.MAX_VALUE);
        System.out.println("c). Переменная типа int занимает " + Integer.BYTES + " байта памяти ("
                + Integer.SIZE + " бит памяти)," + " и принимает значения в диапазоне от "
                + Integer.MIN_VALUE + " до " + Integer.MAX_VALUE);
        System.out.println("d). Переменная типа long занимает " + Long.BYTES + " байт памяти ("
                + Long.SIZE + " бит памяти)," + " и принимает значения в диапазоне от "
                + Long.MIN_VALUE + " до " + Long.MAX_VALUE);
        System.out.println("e). Переменная типа float занимает " + Float.BYTES + " байт памяти ("
                + Float.SIZE + " бит памяти)," + " и принимает значения в диапазоне от "
                + Float.MIN_VALUE + " до " + Float.MAX_VALUE);
        System.out.println("f). Переменная типа double занимает " + Double.BYTES + " байта памяти ("
                + Double.SIZE + " бит памяти)," + " и принимает значения в диапазоне от "
                + Double.MIN_VALUE + " до " + Double.MAX_VALUE);
    }
}
